setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
      as: DATABASE
build:
  languages:
    - go
    - nodejs
  config:
    GOROOT: go
  packages:
    - nodejs
    - golang
run:
  web: ./bin/app 