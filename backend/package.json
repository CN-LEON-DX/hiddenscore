{
  "name": "hiddenscore-backend",
  "version": "1.0.0",
  "description": "Hidden Score Backend API",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "start": "./app",
    "build": "go build -o app cmd/api/main.go",
    "postinstall": "npm run build"
  }
} 