{
  "name": "hiddenscore",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd frontend && npm install --production=false --legacy-peer-deps && npm run build",
    "start": "go run main.go"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "cacheDirectories": ["frontend/node_modules"]
} 