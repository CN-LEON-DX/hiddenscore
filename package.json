{
  "name": "hiddenscore",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd frontend && npm install && npm run build",
    "start": "go run main.go"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=8"
  }
} 